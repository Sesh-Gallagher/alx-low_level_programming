#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: n is the integer to check
 *
 * Return: integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
